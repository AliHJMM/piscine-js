const words = (str) => str.split(' ');
const sentence = (strArr) => strArr.join(' ');
const yell = (str) => str.toUpperCase();
const whisper = (str) => str.toLowerCase();
const capitalize = (str) => (str.toLowerCase()) (str[0].toUpperCase()) ;